<% if user_signed_in? %>
  <%= render partial: 'header'%>
  <%= render partial: 'location_list'%>
  <%= render partial: 'suggestions/form' %>

<% else %>
  <%= button_to "Sign in", new_user_session_path %>
<% end %>
